pyobs-CHANGEME
###############

This is a `pyobs <https://www.pyobs.org>`_ (`documentation <https://docs.pyobs.org>`_) module for
CHANGEME (one line: what hardware/protocol this drives).


Example configuration
**********************

This is an example configuration::

    class: pyobs_CHANGEME.ExampleClass

    # CHANGEME: real options this module actually reads, with realistic values,
    # not a stub. Include comm/vfs/location blocks only if this module needs them.


Available classes
******************

CHANGEME: one subsection per public class, one per line below if there's more than one.

ExampleClass
============
.. autoclass:: pyobs_CHANGEME.ExampleClass
   :members:
   :show-inheritance:
