pyobs-CHANGEME
###############

This is a `pyobs <https://www.pyobs.org>`_ (`documentation <https://docs.pyobs.org>`_) GUI client
for CHANGEME, written in CHANGEME (language/framework, e.g. "C++/Qt/QML" or "TypeScript/Vue").


Example configuration
**********************

CHANGEME: the connection config this client needs (fleet/XMPP endpoint, saved-account file,
whatever it reads on startup) — a real example, not a stub.


Available classes
******************

CHANGEME — Doxygen-backed repos only (via doxysphinx, see docs-structure-by-project-group.md):
include the generated API reference here, e.g.::

    .. toctree::
       :maxdepth: 2

       api/index

Delete this section entirely for repos with no Doxygen/autodoc source (e.g. a TypeScript client
with no doxysphinx-equivalent bridge) — do not leave it as an empty stub.


Views
*****

CHANGEME: one subsection per top-level view this client ships. Each needs a screenshot and a
one-line statement of which module interface(s) it drives.

ExampleView
===========
.. image:: _static/example_view.png
   :alt: ExampleView screenshot

Drives ``IExampleInterface`` — CHANGEME: what the user does with it.


Keyboard shortcuts
*******************

CHANGEME: only if this client binds shortcuts globally (not per-view local ones). Delete this
section if there are none.

===============  ==========================
Shortcut         Action
===============  ==========================
CHANGEME         CHANGEME
===============  ==========================
