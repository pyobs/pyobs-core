# pyobs-CHANGEME

CHANGEME: one paragraph — what this app does and who runs it. Match `docs/source/index.rst`'s
opening paragraph rather than writing a second, divergent description.

## Documentation

Full installation, configuration, and architecture docs: CHANGEME (link to the built Sphinx docs
once they're published, e.g. Read the Docs). Don't duplicate the configuration table or install
steps here — link into `docs/source/installation.rst` / `configuration.rst` instead, so there's
one place to keep them current.

## Development

CHANGEME: the short local-dev quickstart (clone, `uv sync`, how to run tests). Full detail lives
in `docs/source/development.rst`.
