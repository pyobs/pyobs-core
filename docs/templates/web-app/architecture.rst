Architecture
############

CHANGEME: how this app fits into the rest of the pyobs fleet — what it talks to (pyobs-core
modules over XMPP? another web-app over REST?), over what protocol, and why it exists as a
separate service rather than a module. A short diagram earns its place here if there are more
than two or three peers.
