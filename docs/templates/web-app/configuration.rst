Configuration
#############

CHANGEME: every setting this app reads (env var or settings file), with its default and what it
controls. A table, one row per setting, is usually clearest:

===================  =========  =============================
Setting               Default    Purpose
===================  =========  =============================
CHANGEME               CHANGEME   CHANGEME
===================  =========  =============================
