Development
###########

CHANGEME: how to run this app locally against a dev backend, and how to run its test suite
(``uv run pytest``, plus any docker-compose fixtures it needs — e.g. a local ejabberd or
Postgres).
