Installation
############

CHANGEME: how to deploy/run this app for real — Docker image + compose/env file, or bare
``uv run``. Include migrations if Django. Link the actual Dockerfile/compose file in this repo
rather than restating its contents here, so this page doesn't drift from it.
